# Backend environment example (NestJS + TypeScript + Prisma).
# Copy this file to .env and fill in values for your environment.
# **NEVER commit real secrets, passwords, or connection strings.**

# Dev-only admin bootstrap password. Leave empty unless running the
# dev-admin bootstrap command in a local development environment.
SEED_ADMIN_PASSWORD=